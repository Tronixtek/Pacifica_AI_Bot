{
  "name": "vtfx-mt5-bot",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:web": "npm --workspace @vtfx-mt5-bot/web run dev",
    "build:web": "npm --workspace @vtfx-mt5-bot/web run build"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=11.0.0"
  }
}
