{
  "name": "vtfx-mt5-bot",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:web": "npm --workspace @vtfx-mt5-bot/web run dev",
    "build:web": "node scripts/check-build-env.js && npm --workspace @vtfx-mt5-bot/web run build",
    "check:build-env": "node scripts/check-build-env.js"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=11.0.0"
  }
}
